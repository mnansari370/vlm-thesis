# ============================================================================
# Dynamic, Question-Conditioned Visual Token Pruning — LLaVA-1.5 environment
# ----------------------------------------------------------------------------
# This file pins the environment used for the LLaVA-1.5-7B result cells ONLY.
# The Qwen2.5-VL-7B-Instruct cells used a SEPARATE environment (transformers
# 4.51.x + qwen_vl_utils); see requirements-qwen.txt. Do not assume a single
# environment produced every result cell.
#   Python 3.11 · CUDA 12.1 · 2x NVIDIA RTX 6000 Ada (48 GB)
#   Model checkpoint: llava-hf/llava-1.5-7b-hf
#     revision (local cache snapshot used): b234b804b114d9e37bb655e11cbbb5f5e971b7a9
#
# Setup:
#   conda create -n vlm_env python=3.11 -y
#   conda activate vlm_env
#   pip install -r requirements.txt
#
# The torch/transformers/tokenizers pins are RESULT-CRITICAL for the LLaVA
# cells — those numbers were produced with exactly these versions. Do not
# upgrade them if you want to reproduce the reported LLaVA results.
# ============================================================================

# --- Core (result-critical pins) --------------------------------------------
--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.3.0+cu121
torchvision==0.18.0+cu121
transformers==4.46.3
tokenizers==0.20.3
accelerate==1.0.1
sentencepiece==0.2.1
safetensors==0.7.0

# --- Optional model loading ---------------------------------------------------
bitsandbytes==0.49.2        # only needed for the load_in_4bit config option

# --- Data / numerics ----------------------------------------------------------
numpy==2.4.4
pandas==3.0.2
pyarrow==24.0.0             # ScienceQA-IMG parquet loading
pillow==12.2.0
datasets==4.8.4
scipy==1.17.1               # point-biserial confidence correlation
scikit-learn==1.8.0

# --- Config / plotting / misc --------------------------------------------------
PyYAML==6.0.3
matplotlib==3.10.8
tqdm==4.67.3
fvcore==0.1.5.post20221221  # secondary traced-FLOPs estimate (best-effort)
