"""v2 — trained, question-conditioned visual token pruning (thesis Part II).

Self-contained track. Does NOT import from GQA/ or VQA_V2/ (v1 stays frozen);
reusable helpers are COPIED into v2/shared/. Run modules as `python -m v2.<...>`.
"""
